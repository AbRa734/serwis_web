@inject NavigationManager Navigation

<MudButton Variant="Variant.Filled" OnClick="LoginWithGoogle"
           Style="@($"height: 42px; border-radius: 8px; width: 100%; display: flex; align-items: center; justify-content: center;")">
    <i class="fab fa-google" style="margin-right: 16px;"></i> Zaloguj się przez Google
</MudButton>

@code {
    private void LoginWithGoogle()
    {
        Navigation.NavigateTo("/google", true);
    }
}